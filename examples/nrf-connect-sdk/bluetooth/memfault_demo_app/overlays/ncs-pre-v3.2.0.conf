# Pre-v3.2.0, the device-ID choice defaults to CONFIG_MEMFAULT_NCS_DEVICE_ID_STATIC
# which triggers a build-time assertion. Use runtime-setting instead so the build
# succeeds and the device ID is consistent across NCS versions (hw id)
CONFIG_MEMFAULT_NCS_DEVICE_ID_RUNTIME=y
